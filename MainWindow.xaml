<Window x:Class="TeknoParrotBigBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TeknoParrotBigBox"
        Title="{Binding WindowTitle}"
        WindowStyle="None"
        WindowState="Maximized"
        Background="#111111"
        KeyDown="Window_KeyDown"
        Closing="Window_Closing">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <local:BooleanInverterConverter x:Key="BoolInvert" />

        <!-- 绿色主操作按钮样式 -->
        <Style x:Key="GreenPrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3FAE4A" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#2B7A34" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="Bold" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#225A29" />
                    <Setter Property="BorderBrush" Value="#1A3F1E" />
                    <Setter Property="Foreground" Value="#AAAAAA" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 深色次级按钮样式（收藏 / 取消收藏 / 设置 / 关于） -->
        <Style x:Key="DarkActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#303030" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#555555" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#202020" />
                    <Setter Property="BorderBrush" Value="#404040" />
                    <Setter Property="Foreground" Value="#777777" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <!-- 分类列表 -->
            <ColumnDefinition Width="220" />
            <!-- 当前分类下的游戏封面墙 -->
            <ColumnDefinition Width="2*" />
            <!-- 详情 + 预览 -->
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- 最左侧：分类列表 -->
        <Border Grid.Column="0" Margin="10" Background="#181818" CornerRadius="8">
            <ListBox x:Name="CategoriesList"
                     ItemsSource="{Binding Categories}"
                     SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                     SelectionChanged="CategoriesList_SelectionChanged"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="White"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="4"
                                Padding="6"
                                CornerRadius="4"
                                Background="#252525">
                            <TextBlock Text="{Binding Name}"
                                       FontSize="14"
                                       Foreground="White"
                                       TextTrimming="CharacterEllipsis" />
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- 中间：当前分类下的游戏封面列表 -->
        <Border Grid.Column="1" Margin="10" Background="#202020" CornerRadius="8">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Hidden">
                <ListBox x:Name="GamesList"
                         ItemsSource="{Binding SelectedCategory.Games}"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="White"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         PreviewMouseWheel="GamesList_PreviewMouseWheel"
                         SelectionChanged="GamesList_SelectionChanged">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"
                                       Margin="10"
                                       ItemWidth="180"
                                       ItemHeight="260" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5"
                                    CornerRadius="6"
                                    Background="#303030"
                                    BorderBrush="#505050"
                                    BorderThickness="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Image Grid.Row="0"
                                           Margin="6"
                                           Stretch="UniformToFill"
                                           Source="{Binding CoverImagePath}"
                                           SnapsToDevicePixels="True" />
                                    <!-- 收藏星标（叠加在图片之上） -->
                                    <TextBlock Text="★"
                                               FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="#FFD700"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Top"
                                               Margin="0,2,4,0"
                                               IsHitTestVisible="False"
                                               Visibility="{Binding IsFavorite, Converter={StaticResource BoolToVis}}" />
                                    <TextBlock Grid.Row="1"
                                               Margin="6"
                                               Text="{Binding Title}"
                                               TextWrapping="Wrap"
                                               FontSize="14"
                                               FontWeight="Bold"
                                               Foreground="White"
                                               HorizontalAlignment="Center"
                                               TextAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </Border>

        <!-- 右侧：详情 + 预览区域 -->
        <Border Grid.Column="2" Margin="10" Background="#181818" CornerRadius="8">
            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <!-- 固定区域用于游戏介绍，可滚动 -->
                    <RowDefinition Height="2*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- 标题 -->
                <TextBlock Grid.Row="0"
                           Text="{Binding ElementName=GamesList, Path=SelectedItem.Title}"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="White" />

                <!-- 封面大图 + 视频预览 -->
                <Grid Grid.Row="1" Margin="0,16,0,8" Height="260">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="3*" />
                    </Grid.ColumnDefinitions>

                    <!-- 大封面 -->
                    <Border Grid.Column="0"
                            Margin="0,0,8,0"
                            Background="#202020"
                            CornerRadius="6">
                        <Image Margin="8"
                               Stretch="Uniform"
                               Source="{Binding ElementName=GamesList, Path=SelectedItem.CoverImagePath}"
                               SnapsToDevicePixels="True" />
                    </Border>

                    <!-- 视频预览区域 -->
                    <Border Grid.Column="1"
                            Margin="8,0,0,0"
                            Background="#202020"
                            CornerRadius="6"
                            BorderBrush="#404040"
                            BorderThickness="1">
                        <Grid>
                            <MediaElement x:Name="PreviewMedia"
                                          Source="{Binding ElementName=GamesList, Path=SelectedItem.VideoPath}"
                                          LoadedBehavior="Manual"
                                          UnloadedBehavior="Stop"
                                          Stretch="Uniform"
                                          Volume="0.5" />
                            <!-- 静音切换小喇叭 -->
                            <Button Width="28"
                                    Height="28"
                                    Margin="6"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Bottom"
                                    Background="#66000000"
                                    BorderBrush="Transparent"
                                    Click="ToggleMuteButton_Click">
                                <TextBlock x:Name="MuteIcon"
                                           Text="🔈"
                                           FontSize="14"
                                           Foreground="White"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center" />
                            </Button>
                        </Grid>
                    </Border>
                </Grid>

                <!-- 描述（固定区域 + 可滚动） -->
                <Border Grid.Row="2"
                        Margin="0,8,0,0"
                        Background="Transparent">
                    <!-- 隐藏滚动条，可鼠标滚轮/自动滚动 -->
                    <ScrollViewer x:Name="DescriptionScrollViewer"
                                  VerticalScrollBarVisibility="Hidden"
                                  HorizontalScrollBarVisibility="Disabled"
                                  PanningMode="VerticalOnly"
                                  MouseEnter="DescriptionScrollViewer_MouseEnter"
                                  MouseLeave="DescriptionScrollViewer_MouseLeave">
                        <TextBlock Text="{Binding ElementName=GamesList, Path=SelectedItem.Description}"
                                   Foreground="#DDDDDD"
                                   TextWrapping="Wrap"
                                   FontSize="14" />
                    </ScrollViewer>
                </Border>

                <!-- 底部操作按钮 -->
                <StackPanel Grid.Row="3"
                            Margin="0,12,0,0"
                            Orientation="Horizontal"
                            HorizontalAlignment="Left">
                    <Button x:Name="StartGameButton" Content="开始游戏"
                            Width="100"
                            Margin="0,0,8,0"
                            Height="32"
                            Click="StartGameButton_Click"
                            Style="{StaticResource GreenPrimaryButtonStyle}" />
                    <Button x:Name="FavoriteGameButton" Content="收藏游戏"
                            Width="100"
                            Margin="0,0,8,0"
                            Height="32"
                            Click="FavoriteGameButton_Click"
                            IsEnabled="{Binding ElementName=GamesList, Path=SelectedItem.IsFavorite, Converter={StaticResource BoolInvert}}"
                            Style="{StaticResource DarkActionButtonStyle}" />
                    <Button x:Name="UnfavoriteGameButton" Content="取消收藏"
                            Width="100"
                            Margin="0,0,8,0"
                            Height="32"
                            Click="UnfavoriteGameButton_Click"
                            IsEnabled="{Binding ElementName=GamesList, Path=SelectedItem.IsFavorite}"
                            Style="{StaticResource DarkActionButtonStyle}" />
                    <Button x:Name="BackToParrotButton" Content="返回鹦鹉"
                            Width="100"
                            Margin="0,0,8,0"
                            Height="32"
                            Click="BackToParrotButton_Click"
                            Style="{StaticResource DarkActionButtonStyle}" />
                    <Button x:Name="SettingsButton" Content="设置"
                            Width="80"
                            Margin="0,0,8,0"
                            Height="32"
                            Click="SettingsButton_Click"
                            Style="{StaticResource DarkActionButtonStyle}" />
                    <Button x:Name="AboutButton" Content="关于本程序"
                            Width="110"
                            Margin="0,0,8,0"
                            Height="32"
                            Click="AboutButton_Click"
                            Style="{StaticResource DarkActionButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- 底部提示 + 总游戏数 -->
        <DockPanel Grid.ColumnSpan="3"
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Stretch"
                   Margin="16">
            <TextBlock x:Name="HintText" Text="↑↓←→ 选择    Enter/A 启动    Esc/B 退出    支持 XInput / DINPUT 手柄"
                       Foreground="#BBBBBB"
                       FontSize="14"
                       VerticalAlignment="Center" />
            <TextBlock x:Name="GamesCountText" DockPanel.Dock="Right"
                       Foreground="#BBBBBB"
                       FontSize="14"
                       VerticalAlignment="Center"
                       Text="{Binding GamesCountFormatted, Mode=OneWay}" />
        </DockPanel>
    </Grid>
</Window>

